apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-app
  namespace: latihan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: example-app
  template:
    metadata:
      labels:
        app: example-app
    spec:
      containers:
      - name: example-app
        image: rezahartono/example-app:latest
        ports:
        - containerPort: 80
